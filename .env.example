# Example Vite env file (do not commit real `.env*` files).
# In production (Netlify), prefer setting these as environment variables in the Netlify UI.

# Optional. Overrides the default backend URL.
# VITE_SOCKET_URL=http://localhost:5000
# VITE_SOCKET_URL=https://calling-app-backend-1.onrender.com

# Optional. If backend requires it (note: anything starting with VITE_ is public in the browser build).
# VITE_REMOTE_CONTROL_TOKEN=change-me

# Optional. Used for "download host app" link.
# VITE_HOST_APP_DOWNLOAD_URL=https://github.com/prima-mayank/remote-agent/releases/latest/download/host-app-win.zip

